<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User-Specific Products</title>
    
    <style>
        /* Add your CSS styling here */
        body {
            font-family: Arial, sans-serif;
        }
        h1 {
            color: #333;
        }
        ul {
            list-style-type: none;
            padding: 0;
        }
        li {
            margin-bottom: 20px;
            padding: 10px;
            border: 1px solid #ccc;
            box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
        }
        h3 {
            color: #333;
        }
        p {
            margin: 5px 0;
        }
        img {
            max-width: 100%;
            height: auto;
            width: 150px; 
        }
        a {
            clear: both; /* Ensure it appears below the floated elements */
            /* float: right; */
            margin-top: 10px; /* Add some spacing */
            background-color: #007bff; /* Button background color */
            color: #fff; /* Button text color */
            padding: 5px 10px; /* Button padding */
            border: none; /* Remove button border */
            border-radius: 5px; /* Rounded corners */
            text-decoration: none; /* Remove underlines */
            cursor: pointer; /* Add pointer cursor on hover */
        }
        .header-section {
            background-color: #e42529;
            padding: 20px 0; /* Adjust padding as needed */
            display: flex;
            justify-content: center; /* Horizontally center items */
            align-items: center;
        }
        
    </style>
</head>
<body>
    <div class="header-section">
        <header class="feedback-header">
          <img src="https://www.reliancedigital.in/build/client/images/loaders/rd_logo.svg" alt="Reliance Digital Logo" class="logo">
          <div class="header-content">
            <h1>Welcome to Reliance Digital ,here is your wishlist page</h1>
          </div>
        </header>
      </div>
    <h2>User-Specific Products</h2>
    <ul>
        <% products.forEach((product) => { %>
          <li>
            <p><strong><%= product.product %></strong></p>
            <p><strong>Price</strong> <%= product.price %></p>
            <img src="<%= product.image %>" alt="<%= product.product %>">
            <p><strong>specifications</strong> <%= product.specifications ? product.specifications.join(', ') : 'N/A' %></p>
            <a href="https://www.reliancedigital.in/">View More</a>
          </li>
        <% }); %>
      </ul>
</body>
</html>
